import { WarnButtonDirective } from './warn-button.directive';

describe('WarnButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new WarnButtonDirective();
    expect(directive).toBeTruthy();
  });
});
