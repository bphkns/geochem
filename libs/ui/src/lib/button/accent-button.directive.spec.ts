import { AccentButtonDirective } from './accent-button.directive';

describe('AccentButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AccentButtonDirective();
    expect(directive).toBeTruthy();
  });
});
