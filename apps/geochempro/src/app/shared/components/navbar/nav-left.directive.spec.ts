import { NavLeftDirective } from './nav-left.directive';

describe('NavLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new NavLeftDirective();
    expect(directive).toBeTruthy();
  });
});
