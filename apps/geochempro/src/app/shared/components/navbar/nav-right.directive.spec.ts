import { NavRightDirective } from './nav-right.directive';

describe('NavRightDirective', () => {
  it('should create an instance', () => {
    const directive = new NavRightDirective();
    expect(directive).toBeTruthy();
  });
});
